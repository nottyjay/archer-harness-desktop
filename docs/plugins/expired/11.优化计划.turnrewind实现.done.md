# TurnRewind 插件实施方案（`dsh-tauri-turnrewind`）

> 状态：**已实施**（方案经确认后落地；实施结果与验证证据见文末 §11）
> 关联文档：[docs/spec/TURNREWIND.md](../spec/TURNREWIND.md)（交互稿）、[docs/AGENTS.plugins.md](../AGENTS.plugins.md)（插件规范）
> 归档参考：旧架构 demo `dsh-tauri-turnrewind-ts`（**仅作参考、从未参与实现**）已按需求**删除**；
> 由它提取出的加固项与延后项记录见 §13（提取清单与裁决）与 §13.3（TODO）。

---

## 0. 结论摘要

在 `packages/dsh-tauri-turnrewind/` 重新实现一个 turn 级工作区撤销插件，按本仓库现行「host half / client half」三层插件规范组织：

- **宿主侧**：为每个 Agent turn 在**私有 Git 快照仓**中记录 before / after 两个快照，算出逐文件 `+N -M`，写入每会话 JSON 账本；对外只暴露 2 条 HTTP 路由（summary 读、undo 写）。
- **客户端侧**：接管会话视图的 `conversation.chat.turnTail` 槽位（chain、`priority: -1`），在每一轮结束时渲染变更卡片 —— 「已编辑 N 个文件 / +x -y / 文件清单 / [撤销 ↺] / [审核]」，**撤销是真功能，其余按钮全部占位**。
- **Git 是硬前置**：会话 cwd 不在 Git worktree 内时不做快照，卡片以不可用态呈现，点「撤销」弹出 Dialog 说明原因。
- 不做：阶段一运行中实时 chip、模型提示注入、预览/审核/查看更改、redo、保留策略设置面、恢复围栏。

---

## 1. 需求与已确认决策

| # | 决策点 | 结论 |
|---|---|---|
| 1 | 卡片挂载位置 | `conversation.chat.turnTail`（chain 槽），`priority: -1` **接管**，**替换**官方 `ui-deliverables` 的 “Files changed” 行 |
| 2 | 快照引擎 | **私有 Git 快照仓**（`$DSH_HOME/turnrewind/workspaces/<hash>.git`），自包含存储，**不使用 alternates** |
| 3 | Git 前置 | **必须**位于 Git worktree；非 Git 目录不建快照，点撤销弹 Dialog |
| 4 | 冲突处理 | 预检发现磁盘已被改动 → **拒绝执行并列出冲突文件**（无 `--force`） |
| 5 | 附带项 | 阶段一运行中 chip ❌、撤销后模型提示注入 ❌（先做最小可用） |
| 6 | 文件行点击 | **占位**，不接 `TurnTailOwnerProps.openFile` |
| 7 | 账本 | 每会话 JSON + 原子写（不上 SQLite） |
| 8 | 撤销范围 | 任意有快照记录的 turn 均可撤销；同一 turn 只能撤销一次 |

**非 Git 场景的 Dialog 文案（已给定，逐字实现）**

```text
title: 撤销需要使用 Git 代码仓库
desc : 此操作仅在 Git 代码仓库中运行时有效。
button: [关闭]
```

英文对译（locale 词典）：

```text
title : Undo requires a Git repository
desc  : This action only works when running inside a Git repository.
button: Close
```

---

## 2. 调研结论

### 2.1 参考项目（已克隆到 `source/`，仅参考，不拷贝代码）

| 项目 | 许可 | 取用点 |
|---|---|---|
| `source/dsh-turn-rewind`（Anionex，113★） | BSD-3 | 私有快照仓 + `refs/turnrewind/*` + 临时 index 捕获、冲突分类、工作区路径规范化 |
| `source/dsh-rewind`（Willmylife） | BSD-3 | `agent/pre-step` 屏障 + `write-tree` 快照的最小骨架 |
| `source/deepseek-harness`（官方 **0.1.5-rc.1**，sparse） | MIT | 槽位契约（`turnTail` chain 选举语义）、`ui-deliverables` 的 turn-tail 用法、agent 事件载荷、webserver 路由契约 |
| `source/dsh-auto-collapse`（a179-sanae） | MIT | **可选服务探测**写法（`ctx.inject([...], cb)` + `ctx.get`，避免一次性快照丢能力）、DOM 契约与业务分离 |
| `source/dsh-smooth-stream`（Laplace-bit） | MIT | **跨内核代模块表探测**（见 2.4 陷阱 1）、client store 自兜底、外部插件的 client bundle 纯度校验思路 |

### 2.2 已核实的平台能力

- **宿主钩子**：`agent/pre-step`（waterfall，可 await —— 天然的执行屏障）、`agent/turn-stopping`（serial）、`agent/status`、`agent/error`、`session/event`（`turn/start` / `turn/end`）。
- **客户端槽位**：`conversation.chat.turnTail` 是 **chain** 型、session 作用域，owner props 为 `{ turn: TurnLocation, seq, openFile }`，组件另获 session 标准座（`sessionId` / `useSession`）。
  - **chain 是选举制**：同一轮只渲染一个 entry（升序 `priority`，首个 `select` 非 null 者当选）。
  - 官方 `ui-deliverables` 以默认 `priority: 0` 注册该槽 → 本插件用 `priority: -1` 抢先当选即可稳定接管。
- `select` 契约要求**纯函数**（只读 owner props，禁止读外部可变状态）→ 选举决策只能基于 `owner.turn.turn`，卡片内容由组件内部按 store 状态决定。
- 仓库侧接线已就绪：`packages/dsh-tauri-bundle` 已依赖本包；`src-tauri/resources/internal-plugins.json` 已有条目（**描述文案是旧版两阶段 `/undo` 的，需同步更新**）。

### 2.3 归档版 `dsh-tauri-turnrewind-ts` 的做法与取舍

**它怎么做**：会话 cwd 必须是 Git worktree；私有快照仓 `$DSH_HOME/snapshots/<hash>.git`，通过 `objects/info/alternates` **借用源仓库对象**（附带 `rev-list --missing=print` 连通性自检与「失效即删仓重建基线」自愈）；临时 `GIT_INDEX_FILE` + `add --all` + `write-tree` + `commit-tree` + `update-ref refs/turnrewind/*`；`agent/inbox/claimed` 预留 + `agent/pre-step` await 屏障；`turn/end` 后台结算；SQLite 五表账本；恢复用逐文件 atomic bak-swap；UI 为 `/undo` 两阶段计划 + 确认。

**本方案取其骨架、弃其复杂度**：

| | 归档版 | 本方案 | 原因 |
|---|---|---|---|
| 对象存储 | alternates 借用 | 自包含 | 去掉整套自愈机制；代价是首轮把内容复制进私有仓（受源仓库 ignore 规则约束） |
| 索引 | tmpdir index，seed 源仓真实 index | 私有仓自己的 index（stat 缓存天然增量），首轮 `read-tree --empty` | 不复制用户 index，不写 tmpdir |
| 账本 | SQLite 五表 + quick_check + 备份 + 恢复围栏 | 每会话 JSON 原子写 | MVP 够用 |
| 结算 | `turn/end` 后后台 | `turn/end` 后后台 FIFO（同）+ idle 兜底 | 不阻塞 turn 边界，与参考实现一致 |
| 恢复 | 逐文件 bak-swap → rename | `git checkout <commit> -- <path>` | 由 git 负责写盘 |
| 范围 | redo / 预览 / 通知 / 恢复面板 / doctor / 保留策略 | 只做撤销 | 按需求收敛 |

### 2.4 内核版本适配（**必读**：`0.1.5-rc.1` 与 `0.1.2-rc.1`）

本插件必须同时跑在桌面端当前支持的两个内核上（本机路径）：

```text
0.1.5-rc.1  C:\Users\hairy\AppData\Roaming\io.github.hairyf.deepseek-harness-desktop\dependencies\dsh
0.1.2-rc.1  C:\Users\hairy\AppData\Roaming\io.github.hairyf.deepseek-harness-desktop\dependencies\dsh-0.1.2-rc.1-33729514615
```

上述两个安装目录**只含编译产物、不含 `.d.ts`**，因此契约核对以「官方 0.1.5-rc.1 源码（`source/deepseek-harness`，已 sparse clone）+ 两个内核的编译产物 + 前端 bundle」三方交叉验证为准。已核结论：

**A. 两内核一致、可直接依赖（无需版本分支）**

| 能力 | 0.1.2-rc.1 | 0.1.5-rc.1 | 证据 |
|---|---|---|---|
| `agent/pre-step`（waterfall，可 await） | ✅ | ✅ | agent-loop `dispatch.waterfall("agent/pre-step", …)`；载荷含 `agent`（dispatch 注入） |
| `agent/turn-stopping`（serial，turn 落定**前** await） | ✅ | ✅ | agent-loop `await this.dispatch.serial(...)` |
| `agent/status` / `agent/error` | ✅ | ✅ | dsh-agent `invariant.js` |
| `ctx.sessions`（service `"sessions"`）、`session.header.cwd` | ✅ | ✅ | dsh-session `super(ctx,"sessions")`；session types `readonly cwd?: string` |
| `session/event`（`(session, event)`，`turn/end`） | ✅ | ✅ | dsh-session emit + 官方 0.1.5 源码 |
| `ctx.webServer.register({ kind:'exact', path, handler })` | ✅ | ✅ | dsh-host-webserver `register(route)` |
| 客户端 `conversation.chat.turnTail` 为 **chain**、`select` 必填、`priority` 升序 | ✅ | ✅ | 两内核 `ui-deliverables/lib/client.js` 均以 `select` 注册该槽；前端 bundle 均含 `requires options.select` |
| 官方 `ui-deliverables` 占该槽且 **priority 默认 0** | ✅ | ✅ | 同上（0.1.2 组件名 `ProducedFiles`，0.1.5 为 `Deliverables`） |
| 客户端 locale `register(ns, locale, dict)` 与 `register(ns, { zh, en })` 两种重载 | ✅ | ✅ | 两内核 `dsh-client-locale/lib/client.js` 实现逐字一致 |
| session 槽注册的 inject 工厂首参为 `sessionId` | ✅ | ✅ | 0.1.5 `ui-slots` `InjectParams` = `[sessionId: SessionIdOf, …]` |

**B. 必须适配的差异与陷阱**

1. **模块表陷阱（最高风险，smooth-stream 的 issue #17 曾整站白屏）**
   client bundle 在 `window.__ModuleLoader__` 的 factory 里运行，`require` 只能命中**当前内核实际装载的模块表**。0.1.2 把 snapshot store 从 `dsh-client-runtime` 拆成独立的 `dsh-client-store`；0.1.5 里 `dsh-client-runtime` / `dsh-client-ui-slots` / `dsh-client-store` / `dsh-client-ui-primitives` **都已不是独立包**（并入前端静态平台模块表）。
   → **本插件 client 代码硬约束：零 `@deepseek-ai/*` 值导入**（类型导入可被擦除、无请求）。允许的 bare import 只有 `react` / `react/jsx-runtime` / `dsh-tauri/client` / `dsh-tauri-ui/client`；其余依赖由 `dsh-tauri-tsdown` 内联。
   → `dsh.client.external` **绝不能**写目标内核不存在的包名（那是硬请求）；本插件**不需要任何 external**（只用本仓库的 `dsh-tauri/client`、`dsh-tauri-ui/client` 与平台 react）。
2. **`dsh.client.inject` 只列真实存在的包名**（信息性）：`@deepseek-ai/dsh-client-ui-chat`（`turnTail` 的声明方）、`dsh-tauri`、`dsh-tauri-ui`。
3. **宿主 `inject` 只声明两内核都有的服务**：`webServer`、`sessions`、`agents`。声明星座里不存在的服务会让 fiber **永久 PENDING**（插件静默不生效）——绝不照抄旧插件的 `connection` / `workspaceRegistry` 等清单。
4. **`sessionId` 从注册 inject 工厂参数取，不依赖 `SessionStandardProps.sessionId`**：该类型合并的宿主在版本间搬过家（0.1.0 在 `dsh-client-runtime`，0.1.5 在 `ui-session`/`ui-chat`），而 inject 工厂首参在两个内核里都是框架解析好的 `sessionId`（与 `dsh-tauri-worktree` 现有写法一致）。
5. **可选服务只用 `ctx.inject([...], cb)` + `ctx.get(...)` 探测**，不用一次性 `ctx.get` 快照（auto-collapse 的教训：服务晚于插件 provide 时能力永久丢失）。
6. **槽位缺席即静默不注册**：`ctx.slots.inject(name, setup)` 会等待声明；未来内核若移除 `turnTail`，注册永不发生、不抛错、不白屏（符合本仓库「缺 renderer patch 时优雅降级」的规范）。

---

## 3. 架构总览

```text
用户发消息
  │
  ├─ agent/pre-step (step === 1)
  │     └─【await 屏障】before 快照（write-tree + commit-tree + update-ref）
  │        · 非 Git / 系统目录 / 超限 → 本轮标记 skipped，不阻塞 Agent
  │
  ├─ Agent 改文件（write / edit / bash / pwsh / 格式化器 / 生成脚本…全部按磁盘真实差异纳入）
  │
  └─ session/event: turn/end
        └─ 后台 FIFO（每 workspace 串行）：after 快照 → git diff --numstat → 写账本
           （agent/status → idle 兜底结算被中断的 turn）

客户端 turnTail 卡片（每一轮结束处，priority -1 接管）
  GET  /api/turnrewind/summary?sessionId=…     读本会话全部 turn 记录
  POST /api/turnrewind/undo { sessionId, turn } 冲突预检 → 还原 → 回写账本
```

---

## 4. 宿主侧设计

### 4.1 快照域与私有快照仓

```text
$DSH_HOME/turnrewind/
├─ workspaces/<sha256(规范化 worktree 根)[0:24]>.git/   # 每 worktree 一个私有仓
│     ├─ index                                          # 我们自己持有的索引（增量依据）
│     └─ refs/turnrewind/<sessionId>/<turn>/<before|after>
└─ sessions/<sessionId>.json                            # 账本
```

- **快照域 = Git worktree 根**：由 `git rev-parse --show-toplevel` 解析；会话 cwd 是子目录时归并到根（与归档版一致，共享同一快照域）。
- 私有仓初始化一次：`git init` + `core.worktree = <worktree 根>` + `gc.auto = 0`；此后所有 git 调用一律显式带 `--git-dir=<私有仓>`，**绝不在用户仓库里写任何对象、ref、index、stash**。
- 路径规范化：`realpathSync.native` + Windows/macOS 大小写折叠后再哈希（避免同一目录产生两个快照域）。
- **不使用 alternates**：私有仓自包含，源仓库 `git gc --prune=now` 不会破坏快照。

### 4.2 捕获（before / after 共用一条路径）

```text
git --git-dir=<D> --work-tree=<root> add -A -- .
git --git-dir=<D> --work-tree=<root> write-tree
git --git-dir=<D> --work-tree=<root> commit-tree <tree> [-p <上一次 commit>] -m "turn <n> <phase>"
git --git-dir=<D> --work-tree=<root> update-ref refs/turnrewind/<sessionId>/<turn>/<phase> <commit>
```

- ignore 语义**完全委托给源仓库**（`.gitignore` / global excludes 由 `--work-tree` 自动生效）。
- 并发：**每 workspace 一条 FIFO 队列**串行执行，避免私有仓 index 竞争；完成后清理该 turn 的 ref（账本淘汰或撤销后不再需要时）。
- 失败语义：捕获失败只 `logger.warn` 并给该 turn 记 `snapshotFailed`，**绝不打断 Agent turn**。

### 4.3 差异统计

```text
git --git-dir=<D> diff --numstat --name-status --no-renames <beforeCommit> <afterCommit>
```

- 产出 `{ path, status: 'M' | 'A' | 'D', insertions, deletions, binary }`；二进制文件 insertions/deletions 记为 `null`（UI 显示 `-/-`）。
- `--no-renames`：重命名拆成 `D` + `A`，语义简单、撤销实现无需处理 rename。

### 4.4 账本

`$DSH_HOME/turnrewind/sessions/<sessionId>.json`（原子写：`.tmp` + `rename`，复用 `dsh-tauri` 的宿主存储设施）：

```jsonc
{
  "version": 1,
  "sessionId": "…",
  "workspaceRoot": "D:\\proj",
  "workspaceHash": "…",
  "isGit": true,
  "unavailableReason": null,       // TURNREWIND_GIT_REQUIRED / TURNREWIND_UNSAFE_WORKSPACE / …
  "turns": [
    {
      "turn": 3,
      "turnStartSeq": 41,
      "beforeRef": "refs/turnrewind/…/3/before",
      "afterRef":  "refs/turnrewind/…/3/after",
      "files": [{ "path": "src/a.ts", "status": "M", "insertions": 5, "deletions": 3, "binary": false }],
      "insertions": 18,
      "deletions": 10,
      "createdAt": 1757000000000,
      "undoneAt": null,
      "unavailable": null            // 捕获失败/超限时写原因，卡片降级呈现
    }
  ]
}
```

- 保留上限：每会话 200 条 turn 记录（超出丢弃最早记录并删除其 refs）。

### 4.5 生命周期接线（`host/apply.ts`，全部经 `ctx.effect` / `ctx.on`）

| 时机 | 动作 |
|---|---|
| `agent/pre-step`（waterfall） | 仅 `step === 1`：按 turn 去重后 **await** before 快照（屏障），完成后 `next()`；本轮不可快照（非 Git / 系统目录 / 超限）时直接 `next()` 并标记 skipped |
| `session/event` → `turn/end` | 入队 after 快照 + diff + 账本写入（每 workspace FIFO，后台） |
| `agent/status` → `idle` | 兜底：结算未收到 `turn/end` 的 turn（中断/取消路径） |
| 插件卸载（`ctx.effect` disposer） | 置 disposed、释放等待中的屏障、等待在飞任务收尾 |

### 4.6 HTTP 路由（`host/routes/index.ts`）

统一用 `dsh-tauri` 的 `routeHandler` + `withConnectionAuth`（变更路由自带 loopback 限制与 JSON body 校验）。

```text
GET  /api/turnrewind/summary?sessionId=<id>
  → 200 { isGit, workspaceRoot, unavailableReason, turns: [ { turn, fileCount, insertions, deletions,
            files: [{ path, status, insertions, deletions, binary }], undoneAt, unavailable } ] }

POST /api/turnrewind/undo   { sessionId, turn }
  → 200 { ok: true, restored: [...], removed: [...] }
  → 409 { error: "conflict", conflicts: [ { path, reason } ] }
  → 400 / 403 / 404 / 500
```

校验顺序：`sessionId` 存在 → 其 cwd 解析出的 worktree 根与账本 `workspaceRoot` 一致（会话归属校验）→ turn 记录存在且未撤销 → 冲突预检。

### 4.7 撤销算法与冲突语义

```text
1. 取 before/after commit，git diff --name-status --no-renames
2. 冲突预检（改动任何文件之前）：
     · 状态 M：磁盘当前内容 hash ≠ after 中该路径的 blob hash → conflict
     · 状态 A：磁盘上该路径当前已不是 after 版本（被再改/被删） → conflict
     · 状态 D：磁盘上该路径当前存在（被重建） → conflict
   → 任一冲突：409 + 冲突清单，磁盘不变
3. 无冲突执行：
     · M / D → git --git-dir=<D> --work-tree=<root> checkout <beforeCommit> -- <path>
     · A     → 删除文件，并向上清理变空的父目录（止于 worktree 根）
4. 回写账本 undoneAt；单路径失败计入 failed 明细并如实返回（不谎报成功）
```

### 4.8 守卫、上限与失败语义

- **系统目录拒绝**：家目录本身、家目录祖先、任何盘根 → `TURNREWIND_UNSAFE_WORKSPACE`，不做快照。
- **Git 必需**：`rev-parse --show-toplevel` 失败 → `TURNREWIND_GIT_REQUIRED`，不做快照。
- **上限**：单文件 64 MB、单 turn 5000 文件、单快照 512 MB；超限 → 该 turn 记 `unavailable`，其余 turn 不受影响。
- 任何捕获/统计失败都不抛出到 Agent 链路，只落日志与账本状态。

### 4.9 Git 卫生（零污染保证，测试钉住）

私有仓之外只做**只读** `git rev-parse`。用户仓库的 `HEAD` / 分支 / index / `git status` / refs / stash **必须逐字节不变**（对应测试用例）。绝不调用 `git reset --hard` / `git checkout .` / `git clean` / `git stash`。

### 4.10 宿主目录规划

```text
src/host/
├─ apply.ts              # 钩子接线 + 路由注册
├─ constants/index.ts    # 上限、ref 前缀、错误码
├─ types/index.ts        # HostContext / 账本 / 快照类型
├─ hooks/index.ts        # hookable：turn:captured / turn:undone（事件轴）
├─ routes/index.ts       # GET summary / POST undo
├─ storage/index.ts      # 账本 JSON 原子读写
└─ service/
   ├─ git.ts             # git 子进程封装（simple-git 或 execFile，统一结果类型）
   ├─ workspace.ts       # cwd → worktree 根 + 规范化哈希 + 系统目录守卫
   ├─ snapshot.ts        # before/after 捕获 + numstat 汇总
   ├─ ledger.ts          # turn 记录读写 / 淘汰
   └─ undo.ts            # 冲突预检 + 还原 + 账本回写
```

---

## 5. 客户端侧设计

### 5.1 槽位接管

```ts
ctx.slots.inject('conversation.chat.turnTail', () => ctx.slots.register({
  name: 'conversation.chat.turnTail',
  select: (owner) => ({ turn: owner.turn.turn }),   // 纯函数：只读 owner props，始终当选以接管该槽
  priority: -1,                                      // 抢在官方 ui-deliverables（两内核均默认 0）之前
  locale: NS,
  inject: (sessionId) => ({ sessionId, useSummary: …, undo: … }),  // sessionId 取注册参数，不依赖 SessionStandardProps
}, TurnChangesCard as never))
```

约束（见 2.4-B）：client 代码**零 `@deepseek-ai/*` 值导入**；`select` 保持纯函数（禁止读我们的 store）；槽位若在未来内核中缺席，`slots.inject` 会一直等待而不注册 —— 不报错、不白屏。

**已知后果（需求已确认接受）**：官方 “Files changed” 行不再出现，其「点文件名在右侧栏预览」的行为随之消失。

### 5.2 卡片状态机

| 状态 | 触发条件 | 渲染 |
|---|---|---|
| `ready` | 该 turn 有记录且 `files.length > 0`、未撤销 | 完整卡片（见 5.3） |
| `undone` | 记录 `undoneAt != null` | 卡片 + 「已撤销」徽标，撤销按钮禁用 |
| `empty` | 该 turn 有记录但无文件变化 | `null`（不占位） |
| `unavailable` | `isGit === false` | 不可用态卡片 + 「撤销」按钮（点击弹 Dialog） |
| `failed` | 记录 `unavailable != null`（捕获失败/超限） | 不可用态卡片 + 原因文案 + 「撤销」禁用 |
| `pending` | 该 turn 尚无记录且会话刚结束/仍在跑 | `null` + 触发一次短暂重试（见 5.5） |

### 5.3 UI 结构（对照 `docs/spec/TURNREWIND.md`）

单文件：

```text
┌────────────────────────────────────────────────────────────────────┐
│ [📝]  已编辑 driver.ts                        [撤销 ↺]   [ 审核 ]  │
│       +4 -3                                                        │
└────────────────────────────────────────────────────────────────────┘
```

多文件：

```text
┌────────────────────────────────────────────────────────────────────┐
│ [📝]  已编辑 4 个文件                          [撤销 ↺]   [ 审核 ] │
│       +18 -10                                                      │
├────────────────────────────────────────────────────────────────────┤
│ skills/arch-unplugin/SKILL.md                              +5 -3  │
│ skills/arch-nuxt-module-builder/SKILL.md                   +5 -3  │
│ skills/arch-upkeep/SKILL.md                                +5 -3  │
├────────────────────────────────────────────────────────────────────┤
│ 再显示 1 个文件 ˅                                                  │
└────────────────────────────────────────────────────────────────────┘
```

- 文件清单默认显示 3 行，折叠行「再显示 N 个文件 ˅」是**真功能**（展开/收起）。
- 文件行路径按会话 cwd 缩短（过长省略中段），`title` 放完整路径。
- 全部样式走 `css-render`（`components/turn-changes-card.cssr.ts`，`useMountStyle` 挂载），无 inline style。
- 文案走本插件 locale 命名空间（zh / en 键集一致）。

### 5.4 非 Git Dialog

模态框（`components/git-required-dialog.tsx` + `.cssr.ts`），仅由不可用态卡片的「撤销」按钮打开：

```text
┌────────────────────────────────────────────┐
│ 撤销需要使用 Git 代码仓库                    │
│ 此操作仅在 Git 代码仓库中运行时有效。        │
│                                  [ 关闭 ]  │
└────────────────────────────────────────────┘
```

无障碍：`role="dialog"` + `aria-modal` + 标题/描述关联，Esc 关闭，焦点收敛（参照归档 demo 的 `modal-a11y` 思路，但按本仓库规范重写）。

### 5.5 数据获取与重试

- `store/`：模块级 `createExternalStore`，按 `sessionId` 缓存 summary（状态 + 版本号），组件 `useSyncExternalStore` 订阅（沿用 worktree 插件既有约定）。
- 首次挂载 / `sessionId` 变化 → `GET summary`。
- 检测到「出现新的 `turnEnds` 但账本尚无对应记录」→ **有限重试**（如 600ms × 5），避免 after 快照后台结算期间卡片闪空。
- 撤销成功后本地更新该 turn 为 `undone`（并整表重拉一次校准）。

### 5.6 占位按钮清单（按需求，全部无功能）

| 元素 | 行为 |
|---|---|
| `[📝] 文件` 图标按钮 | 占位（无点击处理） |
| 文件行（hover「查看更改」） | 占位（仅 `title` 文案，点击无动作） |
| `[ 审核 ]` | 占位（无点击处理） |
| `[ 撤销 ↺ ]` | **真功能**：POST undo → 成功刷新、409 展示冲突清单、其它展示错误 |

### 5.7 客户端目录规划

```text
src/client/
├─ index.ts                 # apply：locale + 样式 + 槽位注册（全部经 ctx.effect）
├─ constants/index.ts       # slot 名 / priority / style id / class 前缀 / 重试参数 / effect 标签
├─ types/index.ts           # 跨文件共享类型（summary、卡片状态、props）
├─ locales/index.ts         # zh + en 词典（registerLocale 约定同 worktree 插件）
├─ apis/index.ts(.type.ts)  # 走 dsh-tauri/client 的 fetch
├─ store/index.ts           # 每会话 summary 缓存 + 订阅 + 重试调度
├─ register/turn-tail.ts    # turnTail 槽位注册
├─ components/turn-changes-card.tsx + .cssr.ts
├─ components/git-required-dialog.tsx + .cssr.ts
└─ utils/format.ts          # 路径缩短 / +N -M 文本
```

---

## 6. 文件清单与仓库接线

**新增**（`packages/dsh-tauri-turnrewind/`，约 24 个文件）

```text
package.json(重写)  tsconfig.json  tsdown.config.ts  README.md
src/index.ts  src/shared/constants.ts
src/host/{apply.ts, constants/index.ts, types/index.ts, hooks/index.ts, routes/index.ts, storage/index.ts}
src/host/service/{git,workspace,snapshot,ledger,undo}.ts
src/client/{index.ts, constants/index.ts, types/index.ts, locales/index.ts, apis/index.ts, apis/index.type.ts}
src/client/{store/index.ts, register/turn-tail.ts, utils/format.ts}
src/client/components/{turn-changes-card.tsx, turn-changes-card.cssr.ts, git-required-dialog.tsx, git-required-dialog.cssr.ts}
```

**修改**

| 文件 | 改动 |
|---|---|
| `packages/dsh-tauri-turnrewind/package.json` | 重写为现行规范：`exports` 增加 `./client`；`dsh.client` = `{ platform:'web', inject:['@deepseek-ai/dsh-client-ui-chat','dsh-tauri','dsh-tauri-ui'], external:['dsh-tauri/client','dsh-tauri-ui/client'] }`（**不得出现目标内核不存在的包名**，见 2.4-B1/B2）；依赖 `dsh-tauri` / `dsh-tauri-ui` / `pathe` / `hookable` |
| `packages/dsh-tauri-turnrewind/src/**` | client 侧零 `@deepseek-ai/*` 值导入；宿主 `inject` 仅 `['webServer','sessions','agents']`（2.4-B1/B3） |
| `src-tauri/resources/internal-plugins.json` | `dsh-tauri-turnrewind` 条目描述更新为 **turn 尾部变更卡片 + 一键撤销**（旧文案描述的是两阶段 `/undo` 与恢复面板） |
| `packages/dsh-tauri-bundle/package.json` | 已含本包，无需改动 |
| `docs/spec/TURNREWIND.md` | 实施完成后补一行指向本方案/README（避免两份文档漂移） |

---

## 7. 测试计划

**宿主（`src/host/**/*.test.ts`，Vitest，真实 git 子进程 + 临时目录）**

| 用例组 | 断言 |
|---|---|
| 工作区解析 | worktree 子目录 → 归并到根；非 Git → `TURNREWIND_GIT_REQUIRED`；家目录/盘根 → `TURNREWIND_UNSAFE_WORKSPACE` |
| 快照与统计 | 单文件改 / 多文件改 / 新增 / 删除 / 中文路径 / CRLF / 二进制 → `numstat` 与状态正确 |
| 零污染 | 捕获与撤销前后：用户仓库 HEAD、分支、index 字节、`git status`、refs、stash **完全不变** |
| 撤销 | 修改还原、新增删除、删除恢复、空目录清理、路径逃逸拒绝（`../`、绝对路径、指向外部的 symlink） |
| 冲突 | 预检命中 → 409 且磁盘零改动；无冲突 → 全部还原 |
| 账本 | 原子写（并发下不产生半截 JSON）、保留上限淘汰、`undoneAt` 回写 |
| 上限 | 超文件数/字节 → 记 `unavailable`，turn 不中断 |

**客户端（`src/client/**/*.test.ts`）**

| 用例组 | 断言 |
|---|---|
| `utils/format` | 路径缩短、`+N -M`、二进制 `-/-`、单复数文案键 |
| 卡片状态判定 | `ready` / `undone` / `empty` / `unavailable` / `failed` / `pending` 六态判定（纯函数） |
| locale | zh / en 键集完全一致 |

---

## 8. 验收与验证步骤

```powershell
pnpm --filter dsh-tauri-turnrewind typecheck
pnpm run lint --fix
pnpm run test -- --run
pnpm build            # prebuild → build:plugins，部署到 src-tauri/resources/node_modules
```

**GUI 手测清单**

1. 在 Git 仓库工作区里让 Agent 改 1 个文件 → 卡片出现「已编辑 x.ts +n -m」→ 点撤销 → 文件回到改动前，卡片转「已撤销」。
2. 让 Agent 改 4 个文件（跨子目录）→ 卡片显示 3 行 + 「再显示 1 个文件 ˅」，展开可见全部。
3. 撤销后手工再改同一文件，再点撤销 → 冲突提示，文件未被覆盖。
4. 让 Agent 新建文件 → 撤销后文件被删除；让 Agent 删除文件 → 撤销后文件恢复。
5. 在非 Git 目录会话点「撤销」→ 弹出既定 Dialog，文案逐字一致。
6. 一轮没有任何文件变化 → 该轮不出现卡片。
7. 点「📝」「查看更改」「审核」→ 无任何动作（占位）；确认官方 “Files changed” 行已被本卡片取代。

**双内核冒烟（必做）**：分别在 `0.1.5-rc.1` 与 `0.1.2-rc.1` 上各跑一遍「安装 → 启动 → 改 1 个文件 → 撤销」，核对：

- 浏览器控制台 **无** `missed the module table` / loader 报错；
- 宿主日志能看到插件路由与事件钩子注册成功（fiber 未 PENDING）；
- 卡片位置、撤销、非 Git Dialog 三项行为一致。

---

## 9. 非目标 / 后续可选

- 阶段一「运行中」实时 `+N -M` chip（输入框上方）。
- 撤销/重做后的模型一次性提示注入（告知文件已回滚、需重新读取）。
- 「查看更改」diff 视图、「审核」流程、`[文件]` 打开文件。
- redo、父对话递归撤销、保留策略设置面、恢复围栏、doctor、多 workspace 锁。
- alternates 借对象（省磁盘）与由此而来的自愈机制。

---

## 10. 风险与缓解

| 风险 | 缓解 |
|---|---|
| **模块表陷阱**：client bundle 静态 require 了当前内核没有的 `@deepseek-ai/*` → loader 整棵树失败、界面白屏（smooth-stream 的 issue #17 实例） | client 侧零 `@deepseek-ai/*` 值导入（2.4-B1）；`dsh.client.external` 只写本仓库的 `dsh-tauri/client`、`dsh-tauri-ui/client`；CI/构建期用 dsh-tauri-tsdown 的 external 表兜底；两个内核各跑一次安装冒烟 |
| **服务声明星座不匹配** → 宿主 fiber 永久 PENDING、插件静默失效 | `inject` 只声明两内核都验证过的 `webServer`/`sessions`/`agents`；启动日志显式记录注册结果（2.4-B3） |
| 每轮一次 `git add -A` 的耗时（大仓库明显） | 私有仓 index 的 stat 缓存让后续轮次增量；ignore 交给源仓库（node_modules 天然排除）；超限直接跳过且不阻塞 turn |
| 接管 chain 槽导致官方行（含可点击文件）消失 | 需求已确认接受；后续若要保留点击，只需把文件行接到 `TurnTailOwnerProps.openFile`（一行） |
| 首轮快照把仓库内容复制进私有仓（磁盘） | 自包含是有意取舍；保留上限 + refs 随记录淘汰释放对象，必要时后续引入 alternates |
| after 快照后台结算与卡片渲染竞态 | 客户端对「新一轮无记录」做有限重试；`agent/status → idle` 兜底结算 |
| 未来内核继续漂移（槽位/事件改名） | 只依赖 2.4-A 表中逐条核实过的契约；槽位缺席时 `slots.inject` 静默不注册；`sessionId` 取注册参数而非类型合并（2.4-B4） |

---

## 11. 实施结果（落地记录）

### 11.1 实际交付文件

```text
packages/dsh-tauri-turnrewind/
├─ package.json  tsdown.config.ts  cordis.patch.yml  README.md
├─ src/index.ts                          # barrel：name / inject(webServer,sessions,agents) / API_PREFIX / apply
├─ src/shared/constants.ts               # 插件名 / API 前缀 / TURNREWIND_REASON_GIT_REQUIRED
├─ src/host/
│  ├─ apply.ts                           # pre-step 屏障 + turn/end 结算 + idle 兜底 + 路由 effect
│  ├─ constants/index.ts                 # 上限、错误码、ref 前缀
│  ├─ types/index.ts                     # HostContext / SnapshotStore / TurnRecord / SessionLedger / UndoOutcome
│  ├─ hooks/index.ts                     # hookable：turn:captured / turn:undone
│  ├─ routes/index.ts                    # GET summary / POST undo（routeHandler + withConnectionAuth）
│  └─ service/
│     ├─ git.ts                          # git 子进程封装（用户仓库只读面 / 私有仓读写面分离）
│     ├─ workspace.ts                    # worktree 根解析、键规范化、系统目录守卫、会话查找
│     ├─ snapshot.ts                     # 私有仓初始化 + 捕获 + numstat 差异 + 冲突预检 + 恢复
│     ├─ ledger.ts                       # 每会话 JSON 账本（writeAtomic）+ 会话级串行 + 保留淘汰
│     ├─ undo.ts                         # 归属校验 → 冲突预检 → 恢复 → 回写
│     └─ capture.ts                      # turn 生命周期编排（pre-step 屏障 / 后台结算 / 工作区 FIFO）
└─ src/client/
   ├─ index.ts                           # apply：locale + 槽位注册（零 @deepseek-ai/* 值导入）
   ├─ constants/index.ts  types/index.ts  locales/index.ts（zh+en）  apis/index.ts(.type.ts)
   ├─ store/index.ts                     # 每会话摘要缓存 + 撤销动作 + 有限重试
   ├─ utils/format.ts                    # 纯函数：计数/路径缩短/状态键/卡片状态机/文件裁剪
   ├─ register/turn-tail.ts              # chain 槽注册（priority -1，select 纯函数，sessionId 走 inject 入参）
   └─ components/
      ├─ turn-changes-card.tsx + .cssr.ts
      └─ git-required-dialog.tsx + .cssr.ts
```

测试（5 个文件 / 38 个用例）：`workspace.test.ts`、`snapshot.test.ts`、`ledger.test.ts`、`undo.test.ts`、`client/utils/format.test.ts`。

### 11.2 与方案的偏差（均已在代码注释中说明原因）

| 偏差 | 原因 |
|---|---|
| 未单开 `host/storage/`，账本逻辑与原子写都收在 `service/ledger.ts` | 原子写直接复用 dsh-tauri 的 `writeAtomic`，多一层 storage 目录没有独立职责 |
| 新增方案未列的 `host/service/capture.ts` | turn 生命周期编排（屏障/后台结算/FIFO/失败降级）与「一次快照」是两个职责，混进 `snapshot.ts` 会让两者都难以单测 |
| client 侧没有全局样式 effect | 两个组件各自 `useMountStyle` 自挂自卸，符合「单组件单 cssr 文件」 |
| 折叠行文案带箭头：`再显示 {n} 个文件 ↓` / `收起文件 ↑` | 按需求方补充的样式要求 |
| `packages/dsh-tauri-ui` 新增 `FileText` 图标导出 | 卡片 📝 图标按仓库规则必须由 dsh-tauri-ui 单点内联 Gravity 图标 |
| `src-tauri/resources/internal-plugins.json` 条目描述与 repoUrl 更新 | 旧描述写的是两阶段 `/undo` 与恢复面板 |

### 11.3 验证证据

| 检查 | 结果 |
|---|---|
| `npx eslint packages/dsh-tauri-turnrewind` | 0 error / 0 warning |
| `npx tsc --noEmit`（根，含 `packages/*/src`） | 通过 |
| `npx vitest run packages/dsh-tauri-turnrewind` | 5 文件 / 38 用例全通过 |
| `npx vitest run`（根全量） | 27 文件 / 198 用例全通过 |
| `pnpm --filter dsh-tauri-turnrewind build` | host `dist/index.js` + `dist/index.d.ts`、client `dist/client.cjs`，publint 无问题 |
| **client bundle 模块请求审计** | 仅 `react`、`react/jsx-runtime`、`dsh-tauri/client`、`dsh-tauri-ui/client`；`@deepseek-ai/` 出现次数 **0**（模块表陷阱已排除） |
| 关键测试断言 | 用户仓库 HEAD/分支/refs/status 零污染；恢复后重新快照的树与 before 快照树**逐字节等价**（钉住 CRLF/属性往返对称）；冲突预检命中时磁盘零改动；「用户重建已删文件」也能拦住 |
| `pnpm build:plugins`（根 prebuild → 部署到 `src-tauri/resources/node_modules`） | 见提交时输出（dist 与部署产物不入库） |

### 11.4 仍需人工完成

- **双内核冒烟**：分别在 `0.1.5-rc.1` 与 `0.1.2-rc.1` 上启动桌面端，走「改 1 个文件 → 卡片出现 → 撤销 → 卡片转已撤销」，并确认浏览器控制台**无** `missed the module table`、宿主日志无 fiber PENDING。本机无法在不重启 GUI 的前提下自动切换内核，故这一步留给人工验收。
- 大仓库首轮快照耗时/磁盘占用实测（`git add --all` 全量 vs 后续增量）。

---

## 12. 第二轮：视觉对齐官方 deliverables 行 + 运行中提示条

需求以官方五张截图为准（单文件 / 单文件 hover / 多文件 / 多文件行 hover / 运行中），逐项落实：

| 截图 | 落地 |
|---|---|
| 图一 单文件 | 白底 + 弱描边 + 14px 圆角卡片；左侧 40px 圆角图标块（`FilePlus`）；标题 15px/600 显示文件名；副行绿 `+N` / 红 `-M`；右侧「撤销 ↶」纯文本按钮（`ArrowUturnCcwLeft`）+ 描边胶囊「审核」；单文件**不渲染清单** |
| 图二 单文件 hover | 副行计数换成「查看更改 ↗」（`ArrowUpRight`），纯 CSS 切换（`__card--single:hover`），点击仍无动作（占位） |
| 图三 多文件 | 标题「已编辑 N 个文件」+ 总计数；清单最多 3 行（路径 + 右对齐计数，**去掉状态文字标签**）；底部「再显示 2 个文件 ⌄」（`ChevronDown`） |
| 图四 行 hover | 行整行高亮；本轮删除（D）的文件整行弱化（对应截图中被弱化的那一行） |
| 图五 运行中 | **新增阶段一**：`conversation.input.dock` 居中胶囊「N 个文件已更改 +N -M」，turn 结束后消失 |

**阶段一（运行中提示条）的实现取舍**

- 取数：宿主在 turn 进行期间每 1.5s 跑 `git add --all` + `git diff --numstat <beforeCommit>`，
  把读数缓存进内存（`capture.ts` 的 `entry.live`）；**先刷新私有 index 再 diff**，
  否则本轮新建的文件（还没进 index）会漏计——而「刚创建文件」正是运行中提示最需要反馈的场景。
- 接口：新增 `GET /api/turnrewind/live?sessionId=`，只读内存、不跑 git，客户端轮询频率与
  git 频率解耦；客户端 1.2s 轮询一次，`unmount` 立即停表。
- 生命周期：`beginTurn`（before 快照成功后）起表，`settleTurn` 第一件事就是停表并清读数，
  因此 turn 结束提示条必然消失（读数是过程态，不跨 turn 残留）；插件卸载时清掉所有定时器。
- 槽位：`conversation.input.dock`（list/session，0.1.2-rc.1 与 0.1.5-rc.1 均存在），
  与工作树状态条等 dock 条目并存；不依赖 `InputZone.session` 的字段形状，
  「是否在跑」由宿主回答，owner 份额里的 `running: false` 只用于提前停轮询。

**第二轮验证证据**

| 检查 | 结果 |
|---|---|
| `npx eslint packages/dsh-tauri-turnrewind` | 0 error / 0 warning |
| `npx tsc --noEmit` | 通过 |
| `npx vitest run packages/dsh-tauri-turnrewind` | **7 文件 / 44 用例**全通过（新增 liveDiff 2 例、cssr 形态 2 文件 6 例） |
| `npx vitest run`（根全量） | 29 文件 / **204 用例**全通过 |
| `pnpm build:plugins` | 部署 10 个插件，产物含 chip / counts / `--single` / `/live` 调用 |
| client bundle 模块请求 | 仍只有 `react`、`react/jsx-runtime`、`dsh-tauri/client`、`dsh-tauri-ui/client`，`@deepseek-ai/` 出现 **0 次** |
| 关键新断言 | `liveDiff` 的读数（修改+新建+删除 = 3 文件 +2 -1）与最终 after 差异文件数一致；cssr 形态断言 hover 换行、绿/红配色、14px 圆角/40px 图标块/胶囊按钮 |

**新增文件**：`client/hooks/use-live-changes.ts`、`client/styles/counts.cssr.ts`、
`client/components/{change-counts.tsx, running-changes-chip.tsx, running-changes-chip.cssr.ts}`、
`client/register/running-chip.ts` 及对应测试；`dsh-tauri-ui` 图标 barrel 增补
`FilePlus` / `ArrowUturnCcwLeft` / `ArrowUpRight` / `ChevronDown` / `ChevronUp`（Gravity 单点内联）。
原计划的 `shortenPath` / 状态文字标签（新增/修改/删除）按截图移除——路径改为整行 + CSS 省略号，
状态改由「删除行弱化 + 绿红计数」表达。

### 12.1 验收反馈的三处修正

| 反馈 | 原因 | 修正 |
|---|---|---|
| 撤销后应隐藏撤销按钮，只显示「已撤销」 | 原实现是「徽标 + 禁用按钮」并存，看起来还能再撤一次 | `undone` 时不渲染撤销按钮，只渲染徽标（`__badge`）；`onUndo` 的守卫相应改为 `blocked \|\| undoing` |
| 会话结束后 `__label`（运行中提示条）应隐藏，只留 `__card` | 原实现只靠 1.2s 轮询 + 宿主 `active=false`，turn 收尾卡片可能先出现、提示条后消失 | owner 份额读到 `running: false` 时**立即整条隐藏**（不等下一次轮询），宿主 `active=false` 仍是第二道闸门 |
| 点「再显示 9 个文件」后底部还应有「收起文件」，能回到只显示三个 | **真 bug**：`hiddenCount` 用「当前可见行数」反推，展开后算成 0，折叠控件整块消失、无法收起 | 抽出纯函数 `fileListWindow(files, expanded, limit)`：`hiddenCount` 恒按**折叠态**计算，展开后按钮仍在、文案变「收起文件 ⌃」，点击回到 3 行 |

修正后验证：`vitest run packages/dsh-tauri-turnrewind` **7 文件 / 46 用例**全通过
（`fileListWindow` 新增 3 例，专门钉住「展开后 hiddenCount 仍 > 0」）；根全量 **29 文件 / 206 用例**全通过；
eslint 0 error / 0 warning；`tsc --noEmit` 通过；`pnpm build:plugins` 重新部署成功。

### 12.2 验收反馈的第二批（占位与打开文件）

| 反馈 | 落地 |
|---|---|
| 已撤销时隐藏「审核」与 hover 的「查看更改」 | 「审核」已整体隐藏（见下一行），因此已撤销态自然没有它。**hover 的「查看更改」按验收第二轮反馈恢复为始终显示**（`single && …`，不再排除 undone）：只保留「撤销按钮消失 + 无审核」两条差异 |
| 暂时隐藏「审核」按钮 | 按钮实现整体注释保留，位置留 `TODO(review-action)`；注释里写明「重新启用时，已撤销的 turn 不应显示它」。字典里的 `review` 文案保留，恢复只需取消注释 |
| 点击单文件 / 多文件某一行触发 `openFile`（临时逻辑） | `TurnTailOwnerProps` 增加 `openFile?: (path: string) => void`；单文件标题渲染成 `<button class="__title--link">`、清单行渲染成 `<button class="__file">`，点击调用 `openFile(path)`（相对路径由框架按会话 cwd 解析）。**框架未派发 `openFile` 时退化为纯文本 / `<div>`**，不报错也不白屏；行按钮的基座在 cssr 里重置，视觉与之前的行一致 |

同时把「撤销 ↶」图标的手工 inline `style={{ marginTop: '1px' }}` 收进 cssr
（`c('& svg', { marginTop: '1px' })`）——仓库规范禁止 React 静态 inline style。

本批验证：eslint **0 error / 0 warning**；`tsc --noEmit` 通过；包内 46 用例、根全量 206 用例全通过；
`pnpm build:plugins` 部署成功，产物模块请求仍只有 `react`、`react/jsx-runtime`、
`dsh-tauri/client`、`dsh-tauri-ui/client`（`@deepseek-ai/` 0 次）。

### 12.3 验收反馈的第三批（hover 范围、暂缓交互、字号）

| 反馈 | 落地 |
|---|---|
| 多文件的 `__head` 也要有 hover 显示「查看更改」 | 记录为恢复时的硬要求：hover 规则从 `__card--single:hover` 改为**整卡** `&:hover`（一条规则同时覆盖单文件与多文件头部），并写进 `TODO(view-changes-hover)` 注释 |
| hover 效果与「打开文件」暂时全部停用（留 TODO），只显示 `+xx -x` | TSX：`__hint` 标记整体注释；标题回到 `<span>`、清单行回到 `<div>`，`openFile` / `singlePath` 声明一并注释；`ArrowUpRight` 暂时从 import 移除（TODO 里写明恢复要重新引入）。CSSR：hover 换行规则与 `__title--link`、行按钮基座重置分别注释，各自留 `TODO(view-changes-hover)` / `TODO(open-file)`。功能开关不留运行时常量，恢复就是取消注释 |
| 整体字号偏大 | 全卡收小一档：标题 15→13、副行/清单行/「再显示」13、撤销与审核按钮 13/高 28、徽标 12→11、提示行 12、卡片根 13→12；图标块 40→36px、内边距 10/12 → 8/10；运行中提示条同步 14→13、高 30→28 |

本批验证：eslint 0/0；`tsc --noEmit` 通过；包内 **47 用例**、根全量 **207 用例**全通过
（cssr 测试新增「字号收小」用例，并把原 hover 换行断言改为钉住「当前已停用」的状态）。

## 13. 第四轮：从归档 demo 提取的加固项（B1–B8 + A1–A2 已落地）

### 13.0 缘起、清单与裁决

按需求复核旧架构 demo `dsh-tauri-turnrewind-ts`（**仍只作参考、不拷贝代码**），提取出对现架构
有价值的一组加固项，编号 `B1–B14` 与 `A4`。需求方裁决：

| 裁决 | 内容 |
|---|---|
| 实施 | `B1–B9`，但**必须符合现行架构规范**（pathe / css-render / 槽位与注册规范 / 双内核契约） |
| 延后 | `B10`（其唯一消费方语义未定）；因此 `B9` 一并延后——**不留没有消费方的半成品语义** |
| 记录 | `B10–B14` 与 `A4` 作为 TODO 记录在 §13.3（不承诺实现） |
| 策略 | 嵌套 Git 仓库：**自动跳过并标注**（不静默漏掉，也不把 gitlink 当成可撤销内容） |

> 编号沿用提取清单；§13.1/13.2 按**能力**而非编号逐条对应落地结果，便于日后核对代码。

### 13.1 宿主侧落地

| 能力 | 落地要点 | 主要文件 |
|---|---|---|
| 工作区探测批量化 + 缓存 | `rev-parse --is-inside-work-tree --show-toplevel --git-common-dir` 一条命令取全部；60s TTL 缓存 + 后台刷新 + 上限淘汰；命中 ENOENT（无 git）**不缓存**并回 `GIT_UNAVAILABLE`；`clearWorkspaceProbeCache()` 供测试 | `service/workspace.ts` |
| 敏感路径守卫 | 盘根 / UNC 根 / 家目录按**原始路径形态**判定（pathe `resolve('C:\')` 会得到 `/C:`，故不能只看解析结果）；命中即拒绝建快照并给出原因 | `service/workspace.ts` |
| 撤销的路径安全 | `assertSafeParents` 逐级 `lstat`：父级是符号链接/非目录 → 拒绝穿透；`removeCreatedPath` 只删文件与**空**目录，非空目录拒绝（`REASON_NON_EMPTY_DIR`）；全部在动文件**之前**完成 | `service/paths.ts` + `service/undo.ts` |
| 单文件上限真正生效 | 原先 `MAX_FILE_BYTES` 是死常量。现在 `treeStats` 统计超限条目，超总量时**排除最大的若干文件后重试**（≤`MAX_OVERSIZED_SKIPS`），并把排除清单回传/落盘复用；超限文件过多才如实报不可用 | `service/snapshot.ts` |
| 嵌套仓库自动跳过（A1） | 有界预扫（深度 ≤2 / ≤2000 目录 / 跳过 `.git`、`node_modules`、`.turnrewind`）→ 排除；预扫漏判时用 `git add` 的 `does not have a commit checked out` 报错兜底；成功后做 gitlink 复核，把 `skippedNestedRepos` 回传账本与卡片 | `service/snapshot.ts` |
| 保留策略与容量治理 | 每会话**保留 50 轮**：更老的记录落「已过期」终态（清 `files`、清 refs），超过 **200 条**硬丢弃；私有仓 >2GB → `prune` → 隔离改名 → **轮换代数** → 删隔离目录 → 清排除清单；`gc.auto=0` 后由显式 `git prune --expire=now` 回收不可达对象（实时读数每 1.5s 都在产生它们） | `service/retention.ts` + `service/git.ts` + `service/ledger.ts` |
| 代数（generation）绑定 | 私有仓代数写入 `<gitDir>.json`；账本记录记录捕获时代数；撤销前比对，不一致即落「已过期」终态 | `service/snapshot.ts` + `service/undo.ts` |
| 撤销与捕获串行（A2） | 抽出工作区级 FIFO 队列 `createWorkspaceQueue()`，捕获 / 结算 / 容量治理 / 撤销**全部经它串行**（私有仓 index 是共享可变状态）；队尾结算即出队，避免长期运行下无界增长 | `service/queue.ts` + `service/capture.ts` + `host/routes/index.ts` |
| 冲突预检更完整 | `conflictDetails` 在动文件前一次性给出：本轮之后又被改动的文件、被撤销轮删除后又被重建的文件、以及「文件变目录」这类 git diff 看不见的冲突；命中即 409 且**一个字节都不改** | `service/snapshot.ts` + `service/undo.ts` |
| 失败语义可读 | 新增/区分 `EXPIRED`、`TURN_ACTIVE`、`GIT_UNAVAILABLE`、`UNSAFE_PATH`、`TOO_MANY_OVERSIZED`、`NON_EMPTY_DIR` 等原因；`refs` 消失或代数不符都落**终态**，用户不会每次点击都撞同一个模糊错误 | `shared/constants.ts` + `host/constants/index.ts` + `service/undo.ts` |
| 过期记录不再占用撤销面 | 过期行清空 `files` 与 refs（卡片给出原因，而不是列出「可撤销的文件」却撤不动） | `service/ledger.ts` |

### 13.2 客户端落地

| 能力 | 落地要点 | 主要文件 |
|---|---|---|
| 原因码 → 人话 | 新增纯函数 `reasonKey(reason)`：已知码映射文案键（过期 / 无 git / 轮次仍在跑 / 不安全路径），**未知码原样显示**（宿主先更新时宁可显示生码，也不显示空白或假装成功） | `client/utils/format.ts` + `client/locales/index.ts` |
| 「不在撤销范围内」如实标注 | 卡片新增中性提示行 `__notice--skip`：`N 个超大文件未纳入快照` / `N 个嵌套仓库已跳过`，路径放 `title` 备查。宿主回传条数有上限（20），因此按**计数**呈现 | `client/components/turn-changes-card.tsx` + `.cssr.ts` |
| 撤销失败原因同样走映射 | `undoFailed` 的 `{reason}` 也过 `reasonKey`，不再把 `TURNREWIND_EXPIRED` 直接糊在界面上 | `client/store/index.ts`（消费侧）+ 组件 |

### 13.3 延后项（记录在案，**不承诺实现**）

| 编号 | 内容 | 为什么暂不做 |
|---|---|---|
| `B9` | 会话开局「seed / 基线」的抬升语义 | 唯一消费方是 `B10`，与 B10 同进同退，避免留下没有消费方的半成品语义 |
| `B10` | 敏感文件（密钥/凭据类）进入快照时的提示 | 需要先定「哪些算敏感」的产品口径；误报会让提示失去意义 |
| `B11` | 非 Git 弹窗的焦点陷阱（focus trap）与 Esc 关闭 | 属无障碍打磨，与官方 Dialog 组件的统一改造合并做更省 |
| `B12` | 更多「有界守卫」（扫描/统计类的硬上限） | 现有上限已覆盖主要爆炸面，剩余项需先有实测撑腰 |
| `B13` | 账本版本号 + 自动备份 | 当前账本损坏只影响「撤销记录」，不碰用户仓库；优先级低于可撤销能力本身 |
| `B14` | 工作区漂移绑定（把账本绑定到 inode / 设备号等更强标识） | 现有 `workspaceKey` + 代数 + 会话归属三重校验已覆盖已知场景 |
| `A4` | 撤销中途被打断的日志/栅栏（journal / fence） | 现在的中断语义是「如实上报部分失败且不标记已撤销，可再点一次」；更强的可恢复性需要额外落盘协议 |

### 13.4 本批验证

| 项 | 结果 |
|---|---|
| `npx vitest run packages/dsh-tauri-turnrewind` | **11 文件 / 78 用例**全通过 |
| 新增测试 | `service/queue.test.ts`（5：FIFO 不重叠、跨工作区不阻塞、失败不阻断后续、队尾出队、返回值）；`service/retention.test.ts`（7：清单往返与越界丢弃、失效项自动移出、`prune` 回收不可达对象且不动 ref 链、超限整仓重建 + 代数轮换、隔离残骸清场、`describeRepository`、治理不碰用户仓库）；`service/paths.test.ts`（6）；`snapshot.test.ts` +5（超限排除重试并记明细、自动跳过嵌套仓库、`git add` 报错兜底、代数轮换、被 ref 引用的快照不被 prune）；`undo.test.ts` +2（代数不符 → 过期且一字节不改、轮次仍在跑 → 拒绝）；`format.test.ts` +2（原因码映射 / 未知码返回 `null`） |
| eslint / `tsc --noEmit` | 0 error / 0 warning；两处均通过 |

### 13.5 本批发现并修掉的真 bug

| bug | 现象 | 根因 | 修法 |
|---|---|---|---|
| 代数比对**静默失效** | 撤销前的「快照仓已重建」判定永远不触发，只能靠「ref 不存在」兜底 | 写侧 `rotateGeneration` 写 `<gitDir>.json`，读侧 `readGenerationFor` 读的是 `workspaces/<hash>.json`——**两条路径从来不是同一个文件** | 标记路径只保留一个拼法（`workspaceMarkerPath(store)`），写读共用；新增用例先删标记再重捕，**刻意让 refs 仍然存在**，从而只有代数比对能拦住它（该用例在修复前必失败） |

### 13.6 与方案的偏差（诚实记录）

| 项 | 方案预期 | 实际做法与原因 |
|---|---|---|
| 单文件上限的语义 | 「按单文件内存预算截断」 | 实现为**排除 + 标注 + 保留其余文件的可撤销性**。`git checkout` 是流式的，没有真正的「单文件内存上限」可设；让一个巨大构建产物作废整个工作区的撤销能力，比标注它更糟 |
| 上限的可测性 | 真实上限（64MB / 512MB / 2GB） | 全部提供**选项覆盖**（`CaptureOptions.limits`、`{ maxRepoMb }`），缺省仍是宿主常量；测试用小上限走完同一条代码路径 |
| `GIT_UNAVAILABLE` | 有单测 | **未单测**：模拟「没有 git」需要改进程级 `PATH`，会污染同进程其它用例；已通过探测路径的代码审查与缓存不写入保证覆盖 |

## 14. 第五轮：开放「打开文件」（新核心进侧边栏 / 旧核心静默）

### 14.0 需求与关键调研

> 需求原文：**「开放"打开文件"的功能，但要像官方新核心 `.nArs4W_producedChip` 一样，
> 打开侧边栏的文件，旧核心静默（不打开文件）」**，并追加 **「不要加 hover 样式，
> 就开放功能就好」**。

先核对了两个内核里 `turnTail` owner props 的 `openFile` 到底是什么——**这是本轮最关键的一步**，
因为两个内核都派发它，若只按「有没有 `openFile`」判断，旧核心上会**打开系统程序**：

| 内核 | 派发点 | `openFile(path)` 的实现 | 实际效果 |
|---|---|---|---|
| `0.1.5-rc.1` | `dsh-client-ui-chat/lib/client.js` `TurnTailNodeView` → `renderSlotChain('conversation.chat.turnTail', { turn, seq, openFile })` | `ctx.sidebarRight.openResource(fileAddressFor(sessionId, cwd, path))`（可带 `{ line }`） | **应用内右侧边栏**文本预览页签 |
| `0.1.2-rc.1` | 同上（同样的 `{ turn, seq, openFile }`） | `ctx.remote.session.openWorkspacePath({ path: resolveWorkspacePath(cwd, path) })` | 交给**宿主/系统**打开该路径 |

官方新核心的 `producedChip` 也正是走这条路（`dsh-client-ui-deliverables` 的 `ProducedFiles`：
`<button onClick={() => openFile(path)}>`），README 原文即「Each chip opens the file through the
owner's `openFile`, which the chat view routes to the right Sidebar as a text-preview tab」。

### 14.1 判据：能力探测，不是版本号

`0.1.5-rc.1` 的 `dsh-client-ui-sidebar-right` 会 `ctx.reflect.provide('sidebarRight', controller)`；
`0.1.2-rc.1` 里**连这个包都不存在**（`@deepseek-ai` 下只有 `dsh-client-ui-sidebar`，且不提供
`openResource`）。因此判据取「`sidebarRight` 服务是否存在」：

| 决策 | 落地 |
|---|---|
| 何时探测 | **点击那一刻**，而不是 `apply` 时——`sidebarRight` 由另一个客户端插件发布，apply 顺序不保证它已就位 |
| 怎么探测 | `ctx.reflect.get('sidebarRight')`（cordis 的 reflect 直接查注册表，不受 inject 守卫限制） |
| 为什么不写进 `inject` | 声明式依赖在旧内核上会让**插件加载直接失败**；能力探测缺席时只是退化成「不可点」 |
| 为什么不用 `openFile.length` / 版本号 | 前者是脆弱实现细节，后者与「内核继续漂移」的既定风险直接冲突 |

能力缺席时的渲染也一并处理：标题与清单行渲染成**不可点的普通元素**（`<span>` / `<div>`），
不渲染「点了没反应」的按钮——「静默」应当是**没有假交互**，而不是有交互但什么都不做。

### 14.2 落地

| 文件 | 变更 |
|---|---|
| `client/capabilities/index.ts`（新） | `registerCapabilities(ctx)` / `hasSidebarPreview()`；disposer 只在仍是自己那次安装时清引用 |
| `client/utils/open-file.ts`（新） | 纯函数 `fileOpenHandler({ openFile, sidebarPreview })`：两条都满足才返回处理器；打开失败（同步抛错 / rejected promise）静默——卡片没有错误面，且打开只是便利功能 |
| `client/index.ts` | 新增 `ctx.effect(() => registerCapabilities(cx), …: capabilities)` |
| `client/constants/index.ts` | `TURNREWIND_SIDEBAR_RIGHT_SERVICE = 'sidebarRight'` + effect 标签（注释里写明**为什么绝不能进 `inject`**） |
| `client/components/turn-changes-card.tsx` | 单文件标题 → `<button class="__title--link">`（`title` 放完整路径、`aria-label` 用新文案 `openFile: '打开 {name}'`）；清单行经 `renderFileRow` 二选一渲染（可点 → `<button>`，否则 `<div>`）；删除 `TODO(open-file)` |
| `client/components/turn-changes-card.cssr.ts` | 新增 `__title--link` 与 `__file--open` 的**按钮基座重置**（`border:none` / `background:transparent` / `font-family:inherit` / `text-align:left` / `color:inherit` / `cursor:pointer`）；**刻意不加任何 hover 规则**，并在注释里写明「清单行原有的 `&:hover` 背景是上一轮就有的，本次没有新增」 |
| `client/locales/index.ts` | 中英文各加 `openFile` 一条 |

### 14.3 验证

| 项 | 结果 |
|---|---|
| 新增测试 | `client/capabilities/index.test.ts`（7：未安装 / 新内核有服务 / 旧内核无服务 / 注册表抛错 / 缺 reflect / disposer / 旧实例 disposer 不误清新实例）；`client/utils/open-file.test.ts`（7：可用路径、**旧内核即使有 `openFile` 也一次都不调用**、缺 `openFile`、脏字段、rejected promise 静默、同步抛错静默、路径原样透传） |
| cssr 测试 | 新增一条钉住「只加按钮基座、**没有** `__title--link:hover` / `__file--open:hover`」 |
| 包内 / 根全量 | **13 文件 / 93 用例**；根全量见 §14.6（写于本轮中途，最终数见 14.6） |
| bundle 审计 | 模块请求仍只有 `react` / `react/jsx-runtime` / `dsh-tauri/client` / `dsh-tauri-ui/client`，`@deepseek-ai/` **0 次**；产物内确认调用形态为 `j!==void 0&&M!==void 0 ? <button className="…__title--link" onClick={()=>j(M)}> : <span>`，CSS 里无新增 hover 规则 |

### 14.4 诚实记录

- 「旧核心静默」是用**能力探测**实现的，不是运行时开关：若某个更老/更新的内核同样没有
  `sidebarRight`，行为一样是静默；若某内核提供了该服务，则会打开侧边栏（这才是需求语义）。
- 打开失败被静默吞掉（含 rejected promise）：卡片没有错误展示面，且打开文件只是便利功能。
  若日后要给出失败反馈，应在 store 里加一条错误态，而不是在这里 `console` 输出。
- 本轮**未**恢复 hover 换行（`TODO(view-changes-hover)` 仍保留），也没有给可点元素加下划线等
  hover 视觉——需求明确「不要加 hover 样式」。

### 14.5 验收反馈的修正

| 反馈 | 原因 | 修正 |
|---|---|---|
| 单文件与多文件的标题样式不一样 | **真 bug**：`__title--link` 的基座里写了 `font-size/font-weight/line-height: inherit`，而该规则排在 `__title` **之后**，于是把标题的 13px/600 覆盖成了卡片根的 12px/常规字重 | 基座**只保留 `font-family: inherit`**（按钮不继承字体族；其余排印属性交给 `__title`）；新增 cssr 用例钉住「`--link` 块内不得出现 font-size/font-weight/line-height」，注释里写明原因 |
| 删除归档 `archiving/dsh-tauri-turnrewind-ts` | 该 demo 只用于参考与提取清单（§13.0），清单已落进文档，代码本身不再有价值，留着只会被误当实现参考 | `git rm -r archiving/`；清掉 `eslint.config.mjs` 里随之失效的 `'archiving'` ignore；文档里三处指向该目录的路径改为「已删除的旧架构 demo」 |

### 14.6 本轮验证

| 项 | 结果 |
|---|---|
| `npx vitest run packages/dsh-tauri-turnrewind` | **13 文件 / 94 用例**全通过 |
| `npx vitest run`（根全量） | **35 文件 / 254 用例**全通过（删除 `archiving/` 后无任何引用断裂） |
| `npx tsc --noEmit` / `eslint` | 通过 / 0 error 0 warning |
| 部署 | 按需求**不再执行 `pnpm build:plugins`**（dev 常开，改由重启应用加载）；已部署产物的模块请求与 `@deepseek-ai/` 计数在 §14.3 记录在案 |

## 15. 第六轮：dock 顺序修正 + 「快照失败」的真正根因（被忽略的 exclude pathspec）

本轮由两张验收截图驱动，第二张暴露的是一个**每个 turn 都必然发生**的宿主侧真 bug
（不是偶发失败，也不是「什么都没改所以显示不出来」的表现层问题）。

### 15.1 图一：运行中提示条应排在任务清单与工作树横幅**之上**

已在 `0.1.5-rc.1` 产物里核实的 `conversation.input.dock` 成员（list 型，按 `order` 升序自上而下）：

| 条目 | 注册方 | order |
|---|---|---|
| 本插件提示条（改前 → 改后） | 本插件 | `20` → **`-30`** |
| 官方任务清单（`data-testid="todo-panel"`） | `dsh-client-ui-conversation` | `0` |
| 官方 goal / queue | `dsh-client-ui-goal` / `dsh-client-ui-conversation` | `10` / `20` |
| 工作树插件会话横幅 `.dshp-worktree` | `dsh-tauri-worktree` | `-10` |

原来 order `20` 与官方 queue 同值、落到渲染顺序最后，提示条被任务清单与工作树横幅压在输入框
上方最远处。运行中的实时读数是**当前动作的直接反馈**，应紧贴对话内容，因此改用负值置顶
（`TURNREWIND_RUNNING_CHIP_ORDER = -30`）；constants 注释里记下上表这几个已核实的 order，
避免后续再撞号。

### 15.2 图二：`TURNREWIND_SNAPSHOT_FAILED` 的真实根因

**现象**：什么都没改（用户手动中断），turn 尾部却出现「撤销不可用 / TURNREWIND_SNAPSHOT_FAILED」。

**账本证据**：`$DSH_HOME/dsh-tauri-turnrewind/sessions/session-b6841f32-….json` 里 turn 1 是
`unavailable: TURNREWIND_SNAPSHOT_FAILED`、`beforeRef`/`afterRef` 全空；对应私有仓
（`workspaces/dfc8ef01919a28dadd92cf6b.git`）里能看到 `git add --all` 写下的 index（861 个文件），
但**一根 ref 都没有** —— 说明「加进去了，却在 `update-ref` 之前失败」这类猜测都不成立，
失败发生在整条流水线更早的地方。

**复现**（一次性诊断脚本，临时 `$DSH_HOME`，对用户仓库只读）：

```text
$ npx tsx probe-turnrewind.ts
elapsed ms: 5961
result: { "ok": false, "reason": "TURNREWIND_SNAPSHOT_FAILED" }

# 手工重放同一条命令，拿到被丢弃的 stderr：
$ git --git-dir=<私有仓> --work-tree=<仓库> add --all -- . ':(exclude)source/BongoCat' ':(exclude,glob)source/BongoCat/**' …
The following paths are ignored by one of your .gitignore files:
source
hint: Use -f if you really want to add them.
add exit=1
```

**根因**：`git add --all -- . :(exclude)<path>` 里只要 `<path>` 落在**被 `.gitignore` 忽略的目录**内，
git 就以 “The following paths are ignored by one of your .gitignore files” **退出 1** —— 哪怕它
只是排除项（根本不会被加入）。本仓库 `.gitignore` 忽略了整个 `source/`（参考源码克隆目录），
而 `scanNestedRepos` 又恰好在里面扫到 10 个嵌套仓库，于是每个 turn 的排除清单都必然包含这些路径，
`git add` 必然失败 → **每一轮都记 `unavailable`**。`liveDiff` 用的是同一个 pathspec 拼法，
所以运行中提示条在这类工作区里也永远取不到读数。

**修复**（`host/service/snapshot.ts`）：拼 pathspec 之前先过 `git check-ignore -z --stdin`，
把**已被忽略**的排除路径摘掉（被忽略的路径本来就不会进快照，无需排除）。

- 判定交给 git 自己（与 `git add` 读同一份 index 与忽略规则），**不自制正则**；
- `check-ignore` 用**退出码**表达否定答案（exit 1 = 一条都没命中），因此 `execGit` 的失败分支
  也要回传 stdout（`GitResult` 失败分支新增 `out`），并新增 `input` 选项把路径写进 stdin
  （避开 Windows argv 上限）；
- `captureSnapshot` 的每一轮重试与 `liveDiff` 共用同一个过滤器（捕获、结算、实时读数三条路都不再踩）。

修复后同一仓库、同一条路径：`{ ok: true, commit: b8899f2… }`，861 个文件进快照。

### 15.3 图二的表现层规则：没有基线的通用失败不打扰用户

根因修掉后，「什么都没改」自然回到「无差异 → 不显示卡片」。但**账本里已有的旧行**、
以及将来其它通用失败（捕获子进程被回收、git 暂时报错）仍会渲染成一张写着内部错误码的告警卡片，
因此再加一条判定（`client/utils/format.ts` 的 `resolveCardState`）：

| 条件 | 呈现 |
|---|---|
| `unavailable` 是通用内部失败（`TURNREWIND_SNAPSHOT_FAILED`）且**没有基线**（`hasBaseline === false`） | **不显示卡片**：这一轮从来没有过可撤销的承诺；宿主照常写日志与账本行，诊断不丢 |
| 基线在、after 结算失败（`hasBaseline === true`） | 照常告警：承诺过的撤销落空了，必须说 |
| 超限类（文件数/字节数/超大文件）、过期类、Git 类原因 | 照常呈现（用户能理解、也可能需要采取行动） |

配套改动：

- `recordUnavailable` 在基线确实建立过时保留 `beforeRef`（顺带让保留窗口淘汰时能回收这根孤儿 ref）；
- summary 载荷新增 `hasBaseline`（`host/types` + `host/routes`；客户端类型可选，旧宿主不带该字段时
  客户端保守地照常呈现）；
- `TURNREWIND_SNAPSHOT_FAILED` 提升为跨 half 常量（`shared/constants.ts`）并映射到新文案
  `snapshotFailedReason`，不再把生码糊在界面上。

### 15.4 第三张反馈：手动停止后卡片不出现（结算时序 + 等待窗口）

**反馈**：任务正在运行、已经改过一些文件，用户此时手动停止 → turn 尾部的卡片不出现。

排查出**两个独立成因**，都在这一轮修掉：

**(A) 结算事件早于 before 快照落地 → 这一轮被整个漏掉**

`before` 快照挂在 `agent/pre-step` 的**执行屏障**上（大仓库几秒到几十秒，首次还要初始化私有仓）。
用户在这个窗口里停止时，`turn/end` 与 `agent/status → idle` 都已经到达，而 `active` 里还没有条目：
`settleTurn` / `settleIdle` 于是直接返回，这一轮**再也不会被结算**，直到很久以后某个 idle 才被顺手收掉。

真实账本证据（`$DSH_HOME/dsh-tauri-turnrewind`）：

| 会话 / turn | before ref | after ref | 账本行 |
|---|---|---|---|
| `session-f3f7d1c8` turn 1 | 01:26:31 | **01:56:23** | 01:56:23 才落账（13 个文件） |
| `session-94f4cb70` turn 3 | 01:28:13 | **01:59:19** | 01:59:19 才落账（8 个文件） |
| `session-8d61e1e6` turn 2 | 01:26:00 | 无 | **从未落账** |

修复（`host/service/capture.ts`）：`beginTurn` 把自己登记进 `beginning`（key → 落地 promise），
`settleTurn` 先 `await` 这份 promise 再取条目、`settleIdle` 先等本会话所有在飞的 before 快照。
同时：

- 结算整段按 key 幂等（`settling` 集合）：多了一个让出点后，`turn/end` 与 idle 几乎同时到达时
  必须仍然只结算一次（重复捕 after、重复触发 `onCaptured` 都是实打实的浪费与副作用）；
- 快照跑完时若插件已卸载则不再登记条目（不再留下永不清理的轮询定时器）。

**(B) 客户端等的窗口太短 → 记录来晚了卡片也不出现**

after 快照在 `turn/end` 之后后台结算，前面还可能排着一次在飞的实时读数（每 1.5s 一次
`git add --all` + diff）。原来「700ms × 6 ≈ 4.2s」的重试窗口对大仓库明显不够（实测首次 `add`
要 6–20s），窗口一过卡片就再也不出现（摘要缓存已 ready，没有别的东西会强制刷新）。

修复（`client`）：

- 等待判据从「卡片不可见」改成「**账本里还没有这一轮的记录**」（`hasTurnRecord`）——
  该轮确实没有改动时账本会写一条空记录，那种情况不该继续重试；
- 重试改为指数退避 700ms → 1.4s → 2.8s → 5s 封顶，12 次累计约 50s（`summaryRetryDelayMs` 纯函数，
  单测钉住退避表与异常入参）；
- 新增 `capture.test.ts` 6 例：正常一轮落账、`turn/end` 落在 before 快照飞行中仍被结算、
  idle 同样不丢、两个事件同时到达只结算一次、**idle 之后新开始的一轮不被这次兜底误结算**、
  没有 before 快照的 turn 不会被凭空记一笔。最后那条是这次改动的边界：`settleIdle` 的候选集
  必须在 idle 处理函数**同步进入**时取定，否则等完在飞的快照再回头扫 `active`，就可能把
  idle 之后新开始、其实还在跑的那一轮的 after 拍在 before 刚结束的位置上——它的卡片就再也
  收不到真实改动了。

### 15.5 本轮验证

| 项 | 结果 |
|---|---|
| `npx vitest run packages/dsh-tauri-turnrewind` | **14 文件 / 106 用例**全通过（新增：被忽略路径不炸捕获、实时读数不受影响、捕获与结算时序 6 例、卡片状态机 2 例、原因码与退避 3 例） |
| 根全量 `pnpm run test -- --run` | **37 文件 / 272 用例**全通过 |
| `npx eslint packages/dsh-tauri-turnrewind` | 0 error / 0 warning |
| `pnpm --filter dsh-tauri-turnrewind typecheck` | 通过 |
| `pnpm --filter dsh-tauri-turnrewind build` | host `dist/index.js` + client `dist/client.cjs`，publint 无问题 |
| 真仓库探针（`D:/dsh-tauri-desk/deepseek-harness-desktop`） | 修复前 `SNAPSHOT_FAILED` → 修复后 `ok: true`（861 文件，index 一致） |
| CI | 见 PR 的 `Plugins (vitest + typecheck + lint)` 与 `Rust tests` |

### 15.6 诚实记录

- **被忽略但已被跟踪的路径仍不在快照内**：私有快照仓的 index 从空开始，源仓库里
  「被 `.gitignore` 忽略、但已被 git 跟踪」的路径（本仓库的 `.gitmodules` 与 `source/*` 四个
  gitlink）在私有 index 看来是「未跟踪且被忽略」，`git add --all` 会跳过它们，因此不在撤销范围内，
  当前也不会出现在卡片提示里。这在修改前就存在（本轮只修「pathspec 让整轮失败」），要真正覆盖
  需要把源仓库的跟踪集播种进私有 index，属于独立改动。
- **被忽略的嵌套仓库仍计入 `skippedNestedRepos`**：撤销确实不会碰它们，如实上报优先于「看起来更干净」；
  若验收认为这是噪音，可再单独裁决。
- 图二的第一层修复（§15.2）是行为修复，第二层（§15.3）是呈现裁决：**「没有基线的通用失败」选择沉默**，
  即以「诊断进日志、界面不打扰」为准则。若日后要求任何一次捕获失败都必须可见，应改为在会话里
  给一条非卡片形态的低调提示，而不是恢复这张告警卡片。
- §15.4 的 (A) 与 (B) 是同一现象的两条独立路径：**(A) 让记录不再丢/迟到**（可用账本时间戳证伪），
  **(B) 让客户端等到记录真的落账**（退避窗口是工程折中：50s 覆盖实测 6–20s 的首次 add，
  但不能保证覆盖「超大仓库 + 队列排队」的极端情况）。两者都修掉后，手动停止的 turn 会像正常
  turn 一样在几秒内补齐卡片；若记录迟迟不出现，账本与宿主日志仍能给出确定结论（而不是界面上
  一片空白）。
- `capture.test.ts` 里「结算早于 before 快照」两例只断言**这一轮被记下来了**（基线在、账本行在），
  不断言具体文件——那个窗口里工具还没跑，改动本来就是 0；断言 0 文件会掩盖「整轮消失」这个真问题。

## 16. 第七轮：CodeRabbit 复核的三条 Major

PR #460 的机器人复核提了三条 Major，逐条在本地验证后处置如下（有两条是真问题，第三条只采纳一半）。

| 复核项 | 本地验证 | 处置 |
|---|---|---|
| `liveDiff` 的 `git diff` 没带排除 pathspec：`git add` 不会更新被排除路径的既有 index 条目，于是这些路径的改动会混进运行中实时读数 | **成立**（实测：先在无排除时捕获 `vendor/file.txt`，之后把它加进排除清单并改动 → 不带 pathspec 的 `git diff --numstat <before>` 报 `1 1 vendor/file.txt`；带上 `-- . :(exclude)vendor` 后为 0，且退出码 0、不会触发 `git add` 那条「paths are ignored」错误） | **已修**：`add` 用剔除被忽略项的清单，`diff` 用**完整**排除清单；新增回归测试（已被捕获过、之后才进入排除清单的路径不进实时读数；去掉修复后该测试必然失败） |
| `runBeginTurn` 抛异常（git/IO 错误，而非收敛过的结果对象）时只打日志、不写账本 → 这一轮在账本里永久缺失 | **成立**（`captureSnapshot` 等内部并非处处 try/catch，例如 `mkdir` 失败会直接抛出；`settleTurn` 随后找不到条目） | **已修**：该拒绝路径写一条无基线的 `TURNREWIND_SNAPSHOT_FAILED` 审计行（客户端按 §15.3 保持沉默），并用注入失败的队列补了回归测试 |
| `settleTurn` 在 after 快照/差异/写账本**之前**就删活动条目 → 中途抛错后这一轮再也无法重试 | **成立** | **已修**：只有走到终态（无 store / 无基线 / 账本已写）才移出条目；**账本落定后立刻进终态**，后续清理（删 refs / prune / `onCaptured`）抛错不再重试——否则会重复捕 after、重复触发钩子，还可能把之后才发生的改动算进这一轮。用注入失败的队列钉住「抛错后下一次 idle 兜底能补上」 |
| 建议连 `captureSnapshot` 里的 `git rm --cached` 一起回退（担心被排除路径在 diff 里显示成删除） | **不适用**：那条推论针对的是 `git diff <commit>` 的 index 语义；捕获路径的比较是**树对树**（`write-tree` + `diff <tree> <tree>`），`rm --cached` 正是让「已排除」与「进快照」保持一致所必需的 | **未采纳**（保留现状，理由已写进 `liveDiff` 的注释） |
| Docstring Coverage 60% < 80%（机器人自带的 pre-merge 检查） | 非仓库要求（CI 不含该检查） | 给本轮新增/改动的结算路径函数（`runBeginTurn` / `settleTurn` / `settleIdle`）补了 JSDoc，不追阈值 |

**本轮验证**：`npx vitest run packages/dsh-tauri-turnrewind` **14 文件 / 109 用例**全通过
（新增 3 例：实时读数排除清单、before 抛异常的审计行、结算抛错后的兜底重试）；
`eslint` 0 error / 0 warning；plugin typecheck 与根 `tsc --noEmit` 通过；`pnpm build:plugins` 通过。

## 17. 第八轮：非 Git 工作区彻底静默 + 开放「审核」（新核心打开侧边栏文件树）

### 17.0 需求原文

> 1. 在非 Git 工作区中，每次会话结束都会显示「撤销不可用/该工作区不是 Git 代码仓库，
>    无法记录可撤销的快照。」，应该在非 Git 工作区中啥都不显示
> 2. turnrewind 开放「审查」按钮，在新版本中点击打开侧边栏文件夹，旧版本不显示该按钮

### 17.1 需求一：非 Git 工作区整张卡片不出现

`resolveCardState`（`client/utils/format.ts`）原先在 `!summary.isGit` 时返回
`{ kind:'git-required' }`（卡片副行写「该工作区不是 Git 代码仓库…」，点「撤销」弹说明弹窗），
或把其它拒绝原因返回成 `{ kind:'unavailable' }`。改动后：

| 输入（`isGit === false`） | 改前 | 改后 |
|---|---|---|
| `unavailableReason === 'TURNREWIND_GIT_REQUIRED'` | `{ kind:'git-required' }`（卡片 + 弹窗） | **`{ kind:'hidden' }`** |
| `unavailableReason === null` | `{ kind:'git-required' }` | **`{ kind:'hidden' }`** |
| `TURNREWIND_GIT_UNAVAILABLE`（PATH 上没有 git） | `{ kind:'unavailable' }` | 不变：可操作诊断，仍如实显示 |
| `TURNREWIND_UNSAFE_WORKSPACE`（家目录 / 盘根） | `{ kind:'unavailable' }` | 不变 |

判据边界（为什么只隐藏「本来就没有仓库」这一类）：宿主 `host/routes/index.ts` 里
`isGit = probe.ok || refusedGitWorkspace`，而
`refusedGitWorkspace = !probe.ok && probe.reason !== REASON_GIT_REQUIRED && ledger.isGit`
——「确实是 Git 仓库但被守卫拒绝」本来就是 `isGit === true`，不受本次改动影响；
被隐藏的只有 `GIT_REQUIRED` / 无原因这两支（用户报告的那张噪音卡片）。

连带清理（`git-required` 态从此不可达）：

- 删 `TurnCardState` 的 `{ kind:'git-required' }` 分支与 `resolveCardState` 里对应的分支；
- 删组件 `client/components/git-required-dialog.tsx` + `git-required-dialog.cssr.ts`
  与常量 `TURNREWIND_DIALOG_STYLE_ID`（唯一消费方就是那张卡片）；
- 删随之失效的 4 条文案键（zh / en 同步）：`unavailableGitDesc`、`gitRequiredTitle`、
  `gitRequiredDesc`、`close`；`REASON_KEYS` 里的 `GIT_REQUIRED → unavailableGitDesc` 一并移除；
- 宿主侧 `TURNREWIND_REASON_GIT_REQUIRED`（`shared/constants.ts`、`host/constants`、
  撤销 409 的 error 码）**保留**：协议码照常回传，只是客户端不再为它渲染任何东西。

### 17.2 需求二：开放「审核」

按钮从注释里恢复，并按内核能力分流（依旧不做版本嗅探）：

| 内核 | 能力判据 | 行为 |
|---|---|---|
| `0.1.5-rc.1` | `ctx.reflect.get('sidebarRightTabs')` 的注册表里 `get('files')` 有定义 | 渲染按钮；点击 `ctx.sidebarRight.openTab('files')` → 应用内右侧边栏打开**文件树**（会话工作区根目录，即官方 guide 里的 Files 页） |
| `0.1.2-rc.1` | 连 `dsh-client-ui-sidebar-right` 都没有（两个服务都不存在） | **不渲染按钮** |

为什么判据不是「有没有 `sidebarRight`」：控制器的 `openTab(kind)` 内部会
`this.tabs.get(kind)`，类型未注册时直接抛
`sidebarRight: no tab type is registered as "files"`——那样渲染出来的按钮点下去必然失败，
属于「假交互」。另外 `openTab` 走 `require()`（没有挂载的会话面也会抛），
因此调用侧与「打开文件」同策：**同步抛错与 rejected promise 都静默**（卡片没有错误展示面）。

落地文件：

| 文件 | 变更 |
|---|---|
| `client/constants/index.ts` | 新增 `TURNREWIND_SIDEBAR_RIGHT_TABS_SERVICE='sidebarRightTabs'`、`TURNREWIND_SIDEBAR_FILES_KIND='files'`；删 `TURNREWIND_DIALOG_STYLE_ID` |
| `client/capabilities/index.ts` | 抽出 `readService(name)`（reflect + try/catch 收敛）；新增 `readSidebarRight()`（最小面 `SidebarRightFace{ openTab? }`）与 `hasSidebarFileTree()`（注册表 `get('files')`）；`hasSidebarPreview()` 改为复用 `readSidebarRight()` |
| `client/utils/review.ts`（新） | 纯函数 `reviewOpenHandler({ sidebar, fileTree, kind })`：三条判据都满足才返回处理器；`openTab` **绑定 `this`** 后调用（控制器方法读实例状态） |
| `client/components/turn-changes-card.tsx` | 恢复 `dshp-turnrewind__review` 按钮（`onReview !== undefined` 才渲染，去掉 `data-placeholder`）；已撤销的 turn 仍不显示它；删掉弹窗渲染与 `gitRequired` 分支 |
| `client/types/index.ts` + `client/locales/index.ts` | 按 17.1 清理键集合；`review` 文案保留（zh「审核」/ en「Review」） |

> 需求原文写的是「审查」按钮，但代码、文档与既有视觉稿里这个按钮一直是「审核」（locale 键
> `review`）。本轮**不改文案**，先按行为需求落地；若要改名，只需改 `review` 两条字典值。

### 17.3 本批验证

| 项 | 结果 |
|---|---|
| `npx vitest run packages/dsh-tauri-turnrewind` | **15 文件 / 121 用例**全通过（新增 `utils/review.test.ts` 6 例、`capabilities/index.test.ts` +5 例、`format.test.ts` 非 Git 判定拆成 2 例） |
| 根全量 `pnpm run test -- --run` | **38 文件 / 291 用例**全通过 |
| `pnpm typecheck` / `npx eslint packages/dsh-tauri-turnrewind` | 通过 / 0 error 0 warning |
| `pnpm --filter dsh-tauri-turnrewind typecheck` + `build` | 通过（host `dist/index.js` + client `dist/client.cjs`，publint 无问题） |
| bundle 审计 | client 产物 `@deepseek-ai/` **0 次**；`__review` 按钮、`sidebarRightTabs` 探测、`openTab(...)` 调用与 `onReview !== void 0` 守卫均在产物中确认；`git-required` / `GitRequired` / `unavailableGitDesc` **0 次** |

### 17.4 诚实记录

- **「啥都不显示」只覆盖「非 Git 仓库」这一类**：`GIT_UNAVAILABLE`（没装 git）与
  `UNSAFE_WORKSPACE`（家目录 / 盘根）仍是 `isGit === false`，但按 §13/§15 的既定准则
  「用户能理解、也可能需要采取行动的信息必须如实呈现」保留卡片。若需求方要求这些也静默，
  把 `resolveCardState` 的非 Git 分支整体改成 `{ kind:'hidden' }` 即可（一行）。
- **审核按钮的可见性在渲染时探测**（与「打开文件」一致）：`sidebarRightTabs` 由另一个客户端
  插件发布，理论上存在「卡片先渲染、服务后到位」的窗口；此时按钮缺席，直到卡片因
  store 变更（摘要落账 / 重试）重渲染。不额外加订阅，避免为一个便利入口引入轮询。
- **文案未改名**（见 17.2 末尾的说明）。


